# read the input number n and an input word (they are given on separate lines) and repeat this word exactly n times.
# Don't specify any message when reading the input.

chorus_number = int(input())
chorus = input()
print(chorus*chorus_number)